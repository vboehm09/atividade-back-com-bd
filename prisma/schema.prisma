generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Pet {
  id            Int   @id @default(autoincrement())
  nome          String
  especie       String
  idade         Int
  dono          String

  @@map("pets")  
}